[project]
name = "agentguard-reference-kernel"
version = "0.2.0"
description = "Educational reference kernel for AgentGuard execution integrity"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
